apiVersion: batch/v1
kind: CronJob
metadata:
  name: pipeline-api-prune-runs
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: prune-runs
              env:
                - name: REDIS_HOST
                  value: redis-master.garbo.svc.cluster.local
